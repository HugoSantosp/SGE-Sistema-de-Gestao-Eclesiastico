package com.sg.shared.services;

/**
 * Placeholder — service de email DESATIVADO.
 *
 * O service ativo é com.sg.shared.config.EmailService (usado pelo AuthService).
 * Esta classe existe apenas para não quebrar possíveis imports futuros.
 *
 * Para ativar envio de emails:
 * 1. Adicione JavaMailSender como construtor
 * 2. Configure spring.mail.host/username/password no application.yml
 * 3. Copie a lógica de com.sg.shared.config.EmailService para cá
 */
public class EmailService {
    // Placeholder — sem lógica, sem injeção, sem @Service.
}
